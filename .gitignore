# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
build/
dist/

# Виртуальное окружение
.venv/
venv/

# Секреты/локальные настройки
.env

# Логи
logs/*.log

# Скачанные ML-модели (тяжёлые, каждый ставит себе сам через download_models.py)
models/

# Редакторские lock-файлы (LibreOffice/OpenOffice) — уже случайно попадали в репозиторий
.~lock.*#

# IDE
.vscode/
.idea/

# ОС
.DS_Store

# Результаты работы приложения (клипы, временные файлы)
export/output/

# Веса YOLO, которые ultralytics скачивает в корень при экспорте в ONNX
*.pt

# Музыку кладёт пользователь сам (чужой трек = претензия Content ID на YouTube): в репозиторий не попадает
assets/music/*
!assets/music/README.md

# Локальная база SQLite (настройки, проекты, история) — у каждого своя
database/*.db
database/*.db-*
